// 配置项

// const host = 'http://localhost:5757'  本地

const host = 'https://ppcxovez.qcloud.la' // 测试

const loginUrl = `${host}/weapp/login`

const config = {
  host,
  loginUrl
}

export default config
